# ChurchTools Instance Configuration
# The base URL of your ChurchTools instance
VITE_BASE_URL=https://your-instance.church.tools

# Development Credentials (for local development only)
# ⚠️ SECURITY: Never commit actual credentials to version control
VITE_USERNAME=your-dev-username
VITE_PASSWORD=your-dev-password

# Extension Configuration
# The unique key for this extension in ChurchTools
VITE_KEY=fkoinventorymanagement

# Custom Module ID
# REQUIRED: Create a custom module in ChurchTools admin panel first!
# 1. Go to ChurchTools → Settings → Custom Modules
# 2. Create a new module with key "fkoinventorymanagement"
# 3. Copy the module ID and paste it here
# Note: The app will attempt to fetch this automatically, but may fail depending on permissions
VITE_MODULE_ID=

# Optional: API Configuration
# Maximum number of API requests per minute (ChurchTools rate limit)
VITE_API_RATE_LIMIT=60

# Optional: Feature Flags
# Enable offline support for stock take sessions
VITE_OFFLINE_ENABLED=true

# Enable camera scanning for barcode/QR codes
VITE_CAMERA_SCANNING_ENABLED=true